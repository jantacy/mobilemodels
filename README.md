# Phone Brand & Model Summary

Rely on [KHwang9883's MobileModels](https://github.com/KHwang9883/MobileModels), to output a result of dataframe

To use the result, you can just download the latest file [brand_model.csv](https://github.com/jantacy/mobilemodels/blob/main/brand_model.csv)

---
GitHub Actions is set up to auto-update daily, authenticated automatically via the built-in `GITHUB_TOKEN` (no manual setup needed) — see the [official docs](https://docs.github.com/en/actions/security-guides/automatic-token-authentication). If you fork this repo and run into push permission issues, check that your repo's Settings → Actions → General → Workflow permissions is set to "Read and write permissions".
